# dotfiles

## Installation

macOS:
```bash
./install.sh
```

Linux server:
```bash
sudo ./init_server.sh
```

Both scripts are idempotent - safe to run multiple times.

## tmux plugins

After first run: `Ctrl + B` then `I` to install tmux plugins.
